filter kernel_export4 {
    if source = RTS_STATIC then reject;
    if source = RTS_BGP then {
        accept;
    }
    accept;
}

filter kernel_export6 {
    if source = RTS_STATIC then reject;
    accept;
}

